<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:image="http://www.google.com/schemas/sitemap-image/1.1">

    <!-- Home page -->
    <url>
        <loc>{{ url_for('views.home') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>daily</changefreq>
        <priority>1.0</priority>
    </url>

    <!-- About page -->
    <url>
        <loc>{{ url_for('views.about_page') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.8</priority>
    </url>

    <!-- Products page -->
    <url>
        <loc>{{ url_for('views.products') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.9</priority>
    </url>

    <!-- Blog page -->
    <url>
        <loc>{{ url_for('views.blogs') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.7</priority>
    </url>

    <!-- Contact page -->
    <url>
        <loc>{{ url_for('views.contact') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
    </url>

    <!-- Service pages -->
    <url>
        <loc>{{ url_for('views.showrooms') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.7</priority>
    </url>

    <url>
        <loc>{{ url_for('views.factories') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.7</priority>
    </url>

    <url>
        <loc>{{ url_for('views.retail_shops') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
    </url>

    <url>
        <loc>{{ url_for('views.construction_consultants') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
    </url>

    <url>
        <loc>{{ url_for('views.technical_support') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
    </url>

    <!-- Categories page -->
    <url>
        <loc>{{ url_for('views.categories') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.8</priority>
    </url>

    <!-- Link tree page -->
    <url>
        <loc>{{ url_for('views.link_tree') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.5</priority>
    </url>

    <!-- Dynamic category pages -->
    {% for category in categories %}
    <url>
        <loc>{{ url_for('views.category_products', category_id=category.id) }}</loc>
        <lastmod>{{ category.created_at.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.8</priority>
    </url>
    {% endfor %}

    <!-- Dynamic product pages -->
    {% for product in products %}
    <url>
        <loc>{{ url_for('views.product_single', product_id=product.id) }}</loc>
        <lastmod>{{ product.created_at.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.9</priority>
        {% if product.images %}
        {% for image in product.images %}
        <image:image>
            <image:loc>{{ url_for('static', filename='uploads/' + image.image) }}</image:loc>
            <image:title>{{ product.name }}</image:title>
            <image:caption>{{ product.description[:200] if product.description else product.name }}</image:caption>
        </image:image>
        {% endfor %}
        {% endif %}
    </url>
    {% endfor %}

</urlset>