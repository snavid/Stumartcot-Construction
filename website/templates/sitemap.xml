<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:custom="http://www.example.com/schemas/sitemap-custom">
    <!-- Home page -->
    <url>
        <loc>{{ url_for('views.home') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>daily</changefreq>
        <priority>1.0</priority>
        <custom:description>Welcome to our homepage, your gateway to all our products and services.</custom:description>
    </url>

    <!-- Static pages -->
    <url>
        <loc>{{ url_for('views.products') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.8</priority>
        <custom:description>Browse our wide range of products.</custom:description>
    </url>
    <url>
        <loc>{{ url_for('views.blogs') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.7</priority>
        <custom:description>Read our latest blog posts and updates.</custom:description>
    </url>
    <url>
        <loc>{{ url_for('views.contact') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
        <custom:description>Contact us for inquiries and support.</custom:description>
    </url>
   
    <url>
        <loc>{{ url_for('views.showrooms') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
        <custom:description>Explore our showrooms and locations.</custom:description>
    </url>
    <url>
        <loc>{{ url_for('views.factories') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
        <custom:description>Learn about our manufacturing facilities.</custom:description>
    </url>
    <url>
        <loc>{{ url_for('views.retail_shops') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
        <custom:description>Visit our retail shops for exclusive offers.</custom:description>
    </url>
    <url>
        <loc>{{ url_for('views.construction_consultants') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
        <custom:description>Consult with our construction experts.</custom:description>
    </url>
    <url>
        <loc>{{ url_for('views.technical_support') }}</loc>
        <lastmod>{{ current_time.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>monthly</changefreq>
        <priority>0.6</priority>
        <custom:description>Get technical support for our products.</custom:description>
    </url>

    <!-- Dynamic category pages -->
    {% for category in categories %}
    <url>
        <loc>{{ url_for('views.products', category=category.id) }}</loc>
        <lastmod>{{ category.created_at.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.8</priority>
        <custom:description>{{ category.description | safe }}</custom:description>
    </url>
    {% endfor %}

    <!-- Dynamic product pages -->
    {% for product in products %}
    <url>
        <loc>{{ url_for('views.product_single', product_id=product.id) }}</loc>
        <lastmod>{{ product.created_at.strftime('%Y-%m-%d') }}</lastmod>
        <changefreq>weekly</changefreq>
        <priority>0.9</priority>
        <custom:description>{{ product.description | safe }}</custom:description>
    </url>
    {% endfor %}
</urlset>